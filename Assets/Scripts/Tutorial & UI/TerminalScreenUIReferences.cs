using TMPro;
using UnityEngine;

public class TerminalScreenUIReferences : MonoBehaviour
{

    public GameObject ALANConnected;
    public GameObject UploadPopup;
    public GameObject ScreenBlur;
    public GameObject NeedsConnection;
    public TMP_Text UploadText;
    public GameObject UploadCompleteText;
    public TMP_Text ComputerTerminalText;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
