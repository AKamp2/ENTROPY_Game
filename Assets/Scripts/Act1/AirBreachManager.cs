using UnityEngine;
using System.Collections.Generic;

public class AirBreachManager : MonoBehaviour
{
    public List<AirBreachScript> breachList;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    
    //Saving and loading methods for this script should only need to keep track of 1 bool, which isPoweredOn for the air breach. They should be powered on or off.
    //The script already checks for this status when it's done.
}
